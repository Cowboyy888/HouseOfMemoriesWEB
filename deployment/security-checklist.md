# Security Checklist

- Use environment variables for secrets and never commit them.
- Restrict CORS to approved origins.
- Enable HTTPS termination at the edge or reverse proxy.
- Scan dependencies and container images for vulnerabilities.
- Rotate credentials and use separate secrets per environment.
- Keep audit logging enabled for authentication and payment flows.
